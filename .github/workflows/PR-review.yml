name: Pull Request Review

on:
  pull_request:
    branches: [main]

jobs:
  run-frontend-unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Frontend Review
        uses: ./.github/workflows/frontend-review.yml
